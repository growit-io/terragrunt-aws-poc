output "cloud" {
  description = "Same value as the `cloud` input variable."
  value       = var.cloud
}

output "example" {
  description = "Same value as the `example` input variable."
  value       = var.example
}

output "hello" {
  description = "Same value as the `hello` input variable."
  value       = var.hello
}
